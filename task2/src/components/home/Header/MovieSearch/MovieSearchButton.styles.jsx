import styled from 'styled-components';
import { RedButton } from '@components/global';

const MovieSearchButton = styled(RedButton)`
    padding: 1em 4em;
    margin-left: 1rem;
    margin-right: 1rem;
`;

export { MovieSearchButton };